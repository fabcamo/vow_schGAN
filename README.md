# VOW_SCHGAN

This repository contains code and experiments related to *SchemaGAN* for geotechnical soil schematization (VOW project).

## Setup

Create and activate a virtual environment (Python 3.10):

```bash
py -3.10 -m venv .venv
.\.venv\Scripts\activate
python -m pip install --upgrade pip
